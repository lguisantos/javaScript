const array = ['lucas', 'gustavo', 'angela', 'renata'];

// Existem formas melhores de se trabalhar com arrays
for(i in array){
    console.log(`indice:${i} => valor: ${array[i].toLocaleUpperCase()}`);
}